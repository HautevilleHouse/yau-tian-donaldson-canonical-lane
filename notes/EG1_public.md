# EG1 Public Note

The `EG1` package establishes the positive projected response floor.

Define the projected response operator `E_tau = Pi_resp L_tau Pi_resp` on the admissible response sector.

Raw theorem constant:

`kappa_canonical^(raw) := c_canonical_raw * canonical_density_raw - e_canonical_raw`.

Closure criterion:

- the projected response is finite and theorem-level,
- `kappa_canonical > 0` on the declared admissible tube.

Main interpretation: the admissible closure cannot collapse because the projected Kähler-Einstein response remains strictly positive.
