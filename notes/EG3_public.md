# EG3 Public Note

The `EG3` package establishes compactness and no-collapse spacing.

Raw theorem constant:

`kappa_compact^(raw) := (1 + delta_comp_sup_raw)^(-1)`.

Closure criterion:

- normalized near-failure sequences are precompact,
- restart spacing prevents Zeno collapse,
- admissible windows survive extraction.
